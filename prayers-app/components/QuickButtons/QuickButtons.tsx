/**
 * TODO: רשת כפתורי "מה קורה אצלך?"
 *
 * מציגה: QUICK_BUTTONS (constants/quickButtons.ts)
 * לחיצה → פותח Bottom Sheet עם:
 *   - שם + כרטיס שמור (אם יש) + סכום ברירת מחדל
 *   - כפתור "שלח תפילה + תרום"
 *   → useDonation().quickDonate(prayerSlug, buttonSlug)
 *
 * ראה PrayerCard לדוגמא לסגנון כרטיסים
 */
import React from 'react';
import { View, Text } from 'react-native';

export function QuickButtons() {
  // TODO: לממש
  return (
    <View>
      <Text>QuickButtons — TODO</Text>
    </View>
  );
}
